function solve(arr) {
  let first = arr[0];
  let last = arr[arr.length - 1]; //whatever the lengTH is, .length -1 always gives us the last element.

  console.log(first + last);
}
solve([20, 30, 40]);
