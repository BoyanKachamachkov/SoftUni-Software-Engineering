// map() -
// Създава нов масив, като не пипа оригиналния
// -прилага дадена функция върху всеки елемент от оригиналния масив
// -поставя този елемент в новия ни масив
let numbers = [2, 4, 1, 6, 10];

let doubleNumbers = numbers.map((x) => x * 2); //map ne promenq originalniq masiv

console.log(numbers);
console.log(doubleNumbers);
