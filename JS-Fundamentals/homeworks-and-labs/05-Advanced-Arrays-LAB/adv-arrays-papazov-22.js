// ОПЦИИ ЗА КРАЯ НА МАСИВ:
// push(adds at the end of the array) -добавя чиния накрая и връща дължинат - const slojnost
// pop(removes from the end of the array) -маха чиния накрая и пази стойността - const slojnost
// --------------------------
// ОПЦИИ ЗА НАЧАЛОТО НА МАСИВ:
// shift(adds at the start) - expensive, пренарежда целия масив с 1, за огромни масиви не е добре (1000+) -n slojnost
// unshift(removes from the start) -n slojnost
// --------------------------
// МАНИПУЛИРАНЕ НА МАСИВИ:
// slice() - създаваме нов масив с част от елементите на стар масив
// -приема начало и край
// -не се приема оригиналния масив

// --------------------------
// map() creates a new array by applying a function to every element
