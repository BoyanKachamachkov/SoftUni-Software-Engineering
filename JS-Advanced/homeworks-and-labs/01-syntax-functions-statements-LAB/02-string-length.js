function stringLength(arg1, arg2, arg3) {

    let avgLength = arg1.length + arg2.length + arg3.length;
    console.log(avgLength)
    console.log(Math.floor(avgLength/3))
}


stringLength('chocolate', 'ice cream', 'cake');